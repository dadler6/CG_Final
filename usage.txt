Inside the folder:

To “pull” the most recent version of the repository:

git pull

To add your changes to the repository:

git add . //means add “all”
git commit -m “Message about what your adding”
git push origin master

